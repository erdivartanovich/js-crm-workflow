'use strict'

const sourcePath = '../../../src'
const TargetServiceFactory = require(sourcePath + '/Services/Action/TargetServiceFactory')
const PersonService = require(sourcePath + '/Services/Person/PersonService')

let testObject = null

describe('TargetServiceFactory' , () => {
    // describe('#make()', () => {
    //     it('should return a valid service instance', () => {
    //         testObject = new TargetServiceFactory({target_class: 'persons'})
    //         testObject.make().should.be.instanceOf(PersonService)
    //     })
    // })
})
